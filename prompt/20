read the project and make a detailed plan for making the frontend for this

feat: create dashboard for all the existing api endpoints

stack: react (vite), typescript, tailwindcss, shadcn ui, redux toolkit

use skill: fontend design skill

api entrypoint are avaible at: /src/main.c


---

use purly frontend design skill which is at: .claude/skills/* 
and do not use any styles of previsouly made poc
then redesign the current frontend ui and to meet those standards in frontend design skill

--- 

feat: search in frontend
when user is in any directory and presses any key (A-Z, a-z, 0-9, symbols):
automatically trigger search and these charaters will be entered in serach bar and show filter/searched results
- on escape (pressing esc) search will be cleared and navigates back to the same directory

---

feat: implement material design icons:
repository: https://github.com/material-extensions/material-icons-browser-extension 
references: https://marketplace.visualstudio.com/items?itemName=PKief.material-icon-theme
- do it something like use material icons based on the file name/extesion/mime type etc
similarly like vscode material icon theme

---

fix: update file icons:
description: use icon sizing of file and folder like as in image 
image: genome file system (attached)

---

feat: use same icons we see in vscode
i use this extension: material design icons 
we need exactly the same icons as in vscode

---

fix: do not directly show the file propery/stat info view on right side just on clicking/focusing on the file
- add a properies option in context menu at the end of the context menu list
- on clicking that option show the properies view but not in side panel but in a model

---

feat: make entire file system completely keyboard navigatable

---

- in search we should be able to use keyboard to navigation across file and folder: 
  - by default search input should be focused on search
  - on pressing down arrow we file system first file should be focused
  - and then we can navigate using keyboard across the file system
  - on pressing esc search should be cleared (currently working)
  - on pressing any other key again serch query will be changed 

---
- on chaing directory fileter should get re


---

feat: file viewer ui (delayed)
list of files formats to be supported:
  - image (all image formats which browser can handle)
  - video 
  - audio
  - pdf
  - text (all other formats)
- on double clicking a file if file format is supported we should open it in a new tab
  if not supported in browser then we should show a dialog to user to download it

---


feat: user and disk management
need active root session to manage user and disk
- if there is no active session then do not show user and disk management options in sidebar
- to check whether user is root or not: check thier uid and gid it must be 0 and 0
- add user management (add, read, delete, edit) options
- add disk management (add, read, delete, edit) options


---

feat: allow different paths prefix alias 
- / root
- /home/<user> home
- /home/<user>/.imaginary/trash trash (will be implemented later)

keep these in a file (.imaginary/paths)
- means user can extend the path alias list

for frontend keep the ui/ux exact same as gnome file manager

basically what we want when we right click on any folder context menu should have an option to add to places in the sidebar, and this
information should be placed in server in ~/.imaginary/places

---



--- 

current issues:
- previews 
- large uploads
- address bar not visbile and clickable issue
