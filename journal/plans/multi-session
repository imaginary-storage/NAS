multi session auth:

- platform should support multiple sessions at a time
- like this on client:  
  - http only cookie: session_<user>=<token>

- active session will also be stored in cookie in this format:
  - http only cookie: active_session=<token>/<user>
  - this will be used to select the active session for the current request

- in server sessions will be as ususal stored in sessions/ dir and name will be 
  session_<session_id> (which is currently working)

- /whoami will return details based on active session

- setuid() and setgid() will be used to drop privileges to the user based on current session

- new apis to be added:
  - GET /sessions
    - will return a list of all active sessions and its basic details:
    - resoponse format {session_id, username, expiry_time, last_acces_time}[]
  - DELETE /sessions/<session_id>
    - will delete the session and all its data
  - DELETE /logout
    - will delete the active session cookie
  - POST /sessions/switch/<session_id>
    - will switch the active session cookie to the new session

---

frontend:

- we need to show:
  - danger outline when root session is active

---

1. file shareing b/w users
  - feat/file-sharing
  - user will select a file and folder to share
  - file will be only read only
  - folder can be readable and writable
  - the sharer will select:
    - user to share with
    - file/folder to share
    - read/write access
    - expiry time
    - sharer will be able to revoke the share

2. aws sync
  - feat/aws-sync-folder
  - user will select a folder to sync
  - any changes made to the folder will be synced to the aws s3 and glacier storage
  - it is a aws glacier type sync 
  - mostly we do not need to read anything from aws s3 or glacier
  - we will just push changes to aws s3 and glacier

3. fix: unauthroized error while switching sessions - done

---

4. detailed logging and error handling in backend
  - feat/logging-and-error-handling
  - we need to log all the actions and errors in the backend
  - admin can see the application logs in ui
    - for admins there will be a saperate page to view the logs
    - user's cant view the logs
    - need some server sided event to get the logs
  - bascially we need terminal based setup in ui:
    - we then can use this to show read only logs
    - read and write based shell access

---

security:

deployment:

1. versioning using semver
2. workflow for deploying new version
3. installation/updation script 
4. dockerization of server


